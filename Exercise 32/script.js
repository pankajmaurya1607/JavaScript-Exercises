// Exporting Functions Inline:

// 17. Write a JavaScript program to export a function directly during its definition and import it.


import { add, sub, mul} from "./mathsUtils.js"

console.log(add(4, 5));
console.log(sub(4, 5));
console.log(mul(4, 5));
