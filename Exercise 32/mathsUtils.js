// Exporting functions directly at definition
export function add(a, b) {
  return a + b;
}

export function mul(a, b) {
  return a * b;
}

// You can also do inline arrow functions
export const sub = (a, b) => a - b;
