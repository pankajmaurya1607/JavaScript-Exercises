// Importing from reExports instead of mathUtils directly
import {add as sum, sub as diff, mul as prod} from "./mathsUtils.js"

console.log(sum(5, 3));
console.log(diff(5, 3));
console.log(prod(5, 3));
